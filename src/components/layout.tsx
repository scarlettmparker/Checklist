import { PropsWithChildren } from "react";

/**
 * Top-level layout wrapper. UI to be fleshed out.
 */
const Layout = ({ children }: PropsWithChildren) => {
  return <main>{children}</main>;
};

export default Layout;
