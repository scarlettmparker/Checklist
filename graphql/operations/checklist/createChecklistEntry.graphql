mutation createChecklistEntry($name: String) {
  checklistMutations {
    createChecklist(name: $name) {
      ... on QuerySuccess {
        message
        id
      }
      ... on StandardError {
        message
      }
    }
  }
}
