mutation createChecklistEntry($name: String) {
  checklistMutations {
    createChecklist(name: $name) {
      message
      entry {
        id
        name
        dueAt
        completedAt
        status
        createdAt
        updatedAt
      }
    }
  }
}
