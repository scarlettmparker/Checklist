mutation createChecklistEntry($name: String) {
  checklistMutations {
    createChecklist(name: $name) {
      ... on QuerySuccess {
      __typename
      message
        id
      }
      ... on StandardError {
      __typename
      message
      }
    }
  }
}
