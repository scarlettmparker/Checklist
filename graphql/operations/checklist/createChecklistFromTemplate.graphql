mutation createChecklistFromTemplate($templateId: ID!, $name: String) {
  checklistMutations {
    createChecklistFromTemplate(templateId: $templateId, name: $name) {
      message
      entry {
        id
        name
        dueAt
        completedAt
        status
        createdAt
        updatedAt
      }
    }
  }
}
