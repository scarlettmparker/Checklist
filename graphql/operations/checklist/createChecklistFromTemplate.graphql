mutation createChecklistFromTemplate($templateId: ID!) {
  checklistMutations {
    createChecklistFromTemplate(templateId: $templateId) {
      ... on QuerySuccess {
      __typename
      message
        id
      }
      ... on StandardError {
      __typename
      message
      }
    }
  }
}
