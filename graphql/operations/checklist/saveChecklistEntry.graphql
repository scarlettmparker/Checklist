mutation saveChecklistEntry($input: ChecklistEntryInput!) {
  checklistMutations {
    saveChecklist(input: $input) {
      ... on QuerySuccess {
      __typename
      message
        id
      }
      ... on StandardError {
      __typename
      message
      }
    }
  }
}
