mutation archiveChecklist($id: ID!) {
  checklistMutations {
    archiveChecklist(id: $id) {
      ... on QuerySuccess {
        message
        id
      }
      ... on StandardError {
        message
      }
    }
  }
}
