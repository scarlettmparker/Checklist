mutation archiveChecklist($id: ID!) {
  checklistMutations {
    archiveChecklist(id: $id) {
      message
      entry {
        id
        name
        dueAt
        completedAt
        status
        createdAt
        updatedAt
      }
    }
  }
}
