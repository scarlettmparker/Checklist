mutation detachChecklistObject($source: ID!, $target: ID!, $ownerType: RemoteObjectType) {
  checklistMutations {
    detachObject(source: $source, target: $target, ownerType: $ownerType) {
      message
      id
    }
  }
}
