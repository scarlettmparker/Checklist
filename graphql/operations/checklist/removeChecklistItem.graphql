mutation removeChecklistItem($entryId: ID!, $itemId: ID!) {
  checklistMutations {
    removeItem(entryId: $entryId, itemId: $itemId) {
      message
      entry {
        id
        name
        dueAt
        completedAt
        status
        createdAt
        updatedAt
      }
    }
  }
}
