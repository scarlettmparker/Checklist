mutation completeChecklist($id: ID!) {
  checklistMutations {
    completeChecklist(id: $id) {
      ... on QuerySuccess {
      __typename
      message
        id
      }
      ... on StandardError {
      __typename
      message
      }
    }
  }
}
