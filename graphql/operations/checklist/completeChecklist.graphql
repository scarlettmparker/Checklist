mutation completeChecklist($id: ID!) {
  checklistMutations {
    completeChecklist(id: $id) {
      message
      entry {
        id
        name
        dueAt
        completedAt
        status
        createdAt
        updatedAt
      }
    }
  }
}
