query listChecklistTemplateItems($templateId: ID!) {
  checklistQueries {
    templateItems(templateId: $templateId) {
      id
      templateId
      itemId
      name
      icon
      position
    }
  }
}
