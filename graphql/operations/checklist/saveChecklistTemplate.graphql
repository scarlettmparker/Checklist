mutation saveChecklistTemplate($input: ChecklistTemplateInput!) {
  checklistMutations {
    saveTemplate(input: $input) {
      ... on QuerySuccess {
      __typename
      message
        id
      }
      ... on StandardError {
      __typename
      message
      }
    }
  }
}
