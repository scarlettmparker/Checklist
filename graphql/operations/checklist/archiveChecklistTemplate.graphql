mutation archiveChecklistTemplate($id: ID!) {
  checklistMutations {
    archiveTemplate(id: $id) {
      ... on QuerySuccess {
        message
        id
      }
      ... on StandardError {
        message
      }
    }
  }
}
