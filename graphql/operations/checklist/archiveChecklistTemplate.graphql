mutation archiveChecklistTemplate($id: ID!) {
  checklistMutations {
    archiveTemplate(id: $id) {
      message
      template {
        id
        name
        description
        status
        createdAt
        updatedAt
      }
    }
  }
}
