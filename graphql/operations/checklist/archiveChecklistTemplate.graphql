mutation archiveChecklistTemplate($id: ID!) {
  checklistMutations {
    archiveTemplate(id: $id) {
      ... on QuerySuccess {
      __typename
      message
        id
      }
      ... on StandardError {
      __typename
      message
      }
    }
  }
}
