mutation saveChecklistItem($input: ChecklistItemInput!) {
  checklistMutations {
    saveItem(input: $input) {
      message
      item {
        id
        name
        description
        icon
        categoryId
        lifecycleStatus
        createdAt
        updatedAt
      }
    }
  }
}
