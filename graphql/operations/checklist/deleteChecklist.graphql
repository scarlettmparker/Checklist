mutation deleteChecklist($id: ID!) {
  checklistMutations {
    deleteChecklist(id: $id) {
      ... on QuerySuccess {
      __typename
      message
        id
      }
      ... on StandardError {
      __typename
      message
      }
    }
  }
}
