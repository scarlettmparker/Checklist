mutation saveChecklistCategory($input: ChecklistCategoryInput!) {
  checklistMutations {
    saveCategory(input: $input) {
      ... on QuerySuccess {
        message
        id
      }
      ... on StandardError {
        message
      }
    }
  }
}
