mutation retireChecklistItem($id: ID!) {
  checklistMutations {
    retireItem(id: $id) {
      message
      item {
        id
        name
        description
        icon
        categoryId
        lifecycleStatus
        createdAt
        updatedAt
      }
    }
  }
}
