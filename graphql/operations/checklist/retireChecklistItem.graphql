mutation retireChecklistItem($id: ID!) {
  checklistMutations {
    retireItem(id: $id) {
      ... on QuerySuccess {
      __typename
      message
        id
      }
      ... on StandardError {
      __typename
      message
      }
    }
  }
}
