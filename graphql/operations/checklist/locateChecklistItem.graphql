query locateChecklistItem($id: ID!) {
  checklistQueries {
    item(id: $id) {
      id
      name
      description
      icon
      categoryId
      lifecycleStatus
      createdAt
      updatedAt
    }
  }
}
