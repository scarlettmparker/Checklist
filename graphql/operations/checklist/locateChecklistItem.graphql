query locateChecklistItem($id: ID!) {
  checklistQueries {
    item(id: $id) {
      id
      name
      description
      categoryId
      lifecycleStatus
      createdAt
      updatedAt
    }
  }
}
