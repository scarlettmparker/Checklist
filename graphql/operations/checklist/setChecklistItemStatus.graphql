mutation setChecklistItemStatus($entryId: ID!, $itemId: ID!, $status: String!) {
  checklistMutations {
    setItemStatus(entryId: $entryId, itemId: $itemId, status: $status) {
      ... on QuerySuccess {
      __typename
      message
        id
      }
      ... on StandardError {
      __typename
      message
      }
    }
  }
}
