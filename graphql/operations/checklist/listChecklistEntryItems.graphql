query listChecklistEntryItems($entryId: ID!) {
  checklistQueries {
    entryItems(entryId: $entryId) {
      id
      entryId
      itemId
      name
      icon
      status
      position
    }
  }
}
