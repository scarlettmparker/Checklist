input PaginationInput {
  page: Int
  size: Int
  sortBy: String
  sortDir: SortDirection
}

enum SortDirection {
  ASC
  DESC
}

type PageInfo {
  page: Int!
  size: Int!
  totalPages: Int!
  totalCount: Int!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}
